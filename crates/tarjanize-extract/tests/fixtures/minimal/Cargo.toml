# Minimal single-crate fixture.
#
# Tests basic extraction from a simple crate with one struct and one function.
# The empty [workspace] table prevents detection as part of parent workspace.

[package]
name = "minimal_crate"
version = "0.1.0"
edition = "2024"

[workspace]

[dependencies]
