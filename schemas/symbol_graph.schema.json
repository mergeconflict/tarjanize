{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SymbolGraph",
  "description": "Root structure representing the entire symbol graph of a workspace.",
  "type": "object",
  "properties": {
    "crates": {
      "description": "All crates in the workspace. Crates are represented as their\nrespective root modules",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Module"
      }
    },
    "edges": {
      "description": "Dependency edges between symbols",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Edge"
      },
      "uniqueItems": true
    },
    "workspace_name": {
      "description": "Name of the workspace",
      "type": "string"
    }
  },
  "required": [
    "workspace_name",
    "crates",
    "edges"
  ],
  "$defs": {
    "Edge": {
      "description": "A dependency edge between two symbols.",
      "type": "object",
      "properties": {
        "from": {
          "description": "Fully qualified path of the dependent symbol",
          "type": "string"
        },
        "to": {
          "description": "Fully qualified path of the dependency symbol",
          "type": "string"
        }
      },
      "required": [
        "from",
        "to"
      ]
    },
    "Module": {
      "description": "A module (or crate root) containing symbols and submodules.",
      "type": "object",
      "properties": {
        "name": {
          "description": "Module name",
          "type": "string"
        },
        "submodules": {
          "description": "Child modules, omit if empty",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/Module"
          }
        },
        "symbols": {
          "description": "Symbols defined in this module. A symbol is either a module-level\ndefinition (ra_ap_hir::ModuleDef) or an impl block (ra_ap_hir::Impl)",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Symbol"
          }
        }
      },
      "required": [
        "name",
        "symbols"
      ]
    },
    "Symbol": {
      "description": "A symbol in the crate - either a module-level definition or an impl block.",
      "type": "object",
      "properties": {
        "cost": {
          "description": "Approximate cost (unitless) of compiling this symbol. Given two\nsymbols A and B, the estimated cost of compiling A and B in series\nis A.cost + B.cost, and the estimated cost of compiling A and B in\nparallel is max(A.cost, B.cost)",
          "type": "number",
          "format": "double",
          "minimum": 0.0
        },
        "file": {
          "description": "Path to symbol's file relative to crate root",
          "type": "string"
        },
        "name": {
          "description": "Symbol name. Impl names are as they appear in the source,\ne.g. 'impl Trait for Type'",
          "type": "string"
        }
      },
      "oneOf": [
        {
          "description": "Module-level definition, analogous to ra_ap_hir::ModuleDef",
          "type": "object",
          "properties": {
            "module_def": {
              "type": "object",
              "properties": {
                "kind": {
                  "description": "Symbol kind (function, struct, enum, trait, etc.)",
                  "type": "string"
                },
                "visibility": {
                  "description": "Visibility specifier (pub, pub(crate), etc.). Private if absent",
                  "type": [
                    "string",
                    "null"
                  ]
                }
              },
              "required": [
                "kind"
              ]
            }
          },
          "required": [
            "module_def"
          ]
        },
        {
          "description": "Impl block, analogous to ra_ap_hir::Impl. At least one of self_type or\ntrait must be present to satisfy the orphan rule.",
          "type": "object",
          "properties": {
            "impl": {
              "type": "object",
              "properties": {
                "self_type": {
                  "description": "Fully qualified path to self type. Absent if the self type is in\nanother crate, or for blanket impl.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "trait": {
                  "description": "Fully qualified path to trait. Absent if the trait is in another\ncrate, or for inherent impl.",
                  "type": [
                    "string",
                    "null"
                  ]
                }
              }
            }
          },
          "required": [
            "impl"
          ]
        }
      ],
      "required": [
        "name",
        "file",
        "cost"
      ]
    }
  }
}