[package]
name = "minimal_crate"
version = "0.1.0"
edition = "2024"

# Empty workspace table to prevent this fixture from being detected as part of
# the parent tarjanize workspace.
[workspace]

[dependencies]
