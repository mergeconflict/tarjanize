# Virtual workspace with nested crate directories.
#
# Tests extraction from a workspace with:
# - No root package (pure virtual workspace)
# - Crates nested in a `crates/` subdirectory

[workspace]
members = ["crates/member_a"]
resolver = "2"
