# Maximum line width
max_width = 80

# Use edition 2024 formatting rules
edition = "2024"

# Additional formatting preferences
use_small_heuristics = "Default"

# Import organization (requires nightly: cargo +nightly fmt)
group_imports = "StdExternalCrate"
imports_granularity = "Module"
